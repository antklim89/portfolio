import { FC } from 'react';

import { ProjectType } from '~/types';


const ProjectItem: FC<{project: ProjectType}> = () => {
    return (
        <div>
            ProjectItem
        </div>
    );
};

export default ProjectItem;
